# 別店舗でも使える URL で公開する手順

在庫管理を **インターネット上のURL** で開けるようにする手順です。  
無料の **Fly.io + Neon** を使います。

---

## 完成イメージ

- 共有URL例: `https://shabon-inventory.fly.dev`
- 各店舗のスタッフは **同じURL** をブラウザで開く
- **パスワード** で保護（店舗にだけ教える）
- 在庫データは **全店舗で共通**（1つのデータベース）
- 完全リアルタイムでなくてOK（画面を再読み込みすれば最新）

※ 店舗ごとに在庫を分けたい場合は、後から「店舗名」機能を追加できます。

---

## 手順（約30分）

### 1. 準備

- [Fly.io](https://fly.io) アカウント
- [Neon](https://neon.tech) で Postgres（Connection string を控える）
- `flyctl` インストール: `iwr https://fly.io/install.ps1 -useb | iex`

### 2. デプロイ

1. このフォルダで **`デプロイ.bat`** → **1**
2. または:

```powershell
powershell -File scripts\set_fly_secrets.ps1
flyctl deploy -a shabon-inventory --remote-only
```

3. 環境変数（secrets）:
   - `APP_PASSWORD` = `haizi814`
   - `DATABASE_URL` = Neon の接続文字列
   - `SECRET_KEY` = 自動生成

### 3. URL を店舗に共有

1. URL: **https://shabon-inventory.fly.dev**
2. パスワード: **haizi814**
3. スマホ・タブレット・PC のブラウザから開ける

### 4. 品目データの初回取込

- `data/products.json` がリポジトリにあれば、起動時に自動でDBへ取り込まれます
- Render から移行する場合は `scripts\migrate_db_to_neon.ps1` を参照

---

## ローカル（今まで通り）との併用

| 用途 | 方法 |
|------|------|
| 自分のPCだけ | `起動.bat`（従来通り） |
| 全店舗共有 | Fly.io の固定 URL |

---

## Render からの乗り換え

| 項目 | 旧 | 新 |
|------|----|----|
| 公開先 | Render | **Fly.io** |
| URL | `*.onrender.com` | **`https://shabon-inventory.fly.dev`** |
| DB | Render Postgres | **Neon** |

Fly で問題なく使えたら、Render ダッシュボードで旧サービスを停止してください。

---

## トラブル

| 症状 | 対処 |
|------|------|
| 起動しない | `flyctl logs -a shabon-inventory` |
| ログインできない | `APP_PASSWORD` が `haizi814` か確認 |
| 品目が空 | `data/products.json` と Neon 接続を確認 |

---

## セキュリティのおすすめ

- パスワードは定期的に変更（変更後は `flyctl secrets set APP_PASSWORD=...`）
- URLは関係者以外に公開しない
- 可能なら店舗ごとに共有チャットで送る（メールより安全）
