# 別店舗でも使える URL で公開する手順

在庫管理を **インターネット上のURL** で開けるようにする手順です。  
無料の **Render.com** を使います（クレジットカード不要の無料枠あり）。

---

## 完成イメージ

- 共有URL例: `https://shabon-inventory-xxxx.onrender.com`
- 各店舗のスタッフは **同じURL** をブラウザで開く
- **パスワード** で保護（店舗にだけ教える）
- 在庫データは **全店舗で共通**（1つのデータベース）

※ 店舗ごとに在庫を分けたい場合は、後から「店舗名」機能を追加できます。

---

## 手順（約30分）

### 1. GitHub にコードを置く（推奨）

1. [GitHub](https://github.com) でアカウント作成
2. 新しいリポジトリを作成（例: `shabon-inventory`）
3. `shabon-inventory-app` フォルダの中身をアップロード  
   （`.venv` と `backups` は不要）

### 2. Render でデプロイ

1. [Render.com](https://render.com) にログイン（GitHub連携可）
2. **New +** → **Blueprint**
3. GitHub リポジトリを選択
4. `render.yaml` を読み込んで **Apply**
5. 環境変数 **APP_PASSWORD** に、店舗共有用のパスワードを設定（英数字8文字以上推奨）
6. デプロイ完了まで待つ（5〜10分）

### 3. URL を店舗に共有

1. Render のダッシュボード → サービス **shabon-inventory** → 上部の URL をコピー  
   例: `https://shabon-inventory.onrender.com`
2. 各店舗に **URL** と **パスワード** を伝える
3. スマホ・タブレット・PC のブラウザから開ける

### 4. 品目データの初回取込

クラウド版では Excel 自動取込はサーバー上では動きません。次のいずれかで行います。

- **方法A**: いまのPC版で在庫・品目を整えてから、DBを移行（要サポート）
- **方法B**: デプロイ後、各店舗で「品目編集」から登録
- **方法C**: `data/products.json` をリポジトリに含めて再デプロイ（既に96品目入り）

`data/products.json` はリポジトリに含めておけば、起動時に自動でDBへ取り込まれます。

---

## 無料プランの注意

| 項目 | 内容 |
|------|------|
| スリープ | 15分アクセスがないと停止。最初のアクセスで20秒ほど起動待ち |
| データ | PostgreSQL に保存（消えにくい） |
| セキュリティ | 必ず **APP_PASSWORD** を設定すること |

常時すぐ開きたい場合は Render の有料プラン（月額）を検討してください。

---

## ローカル（今まで通り）との併用

| 用途 | 方法 |
|------|------|
| 自分のPCだけ | `起動.bat`（従来通り） |
| 全店舗共有 | クラウド URL |

---

## トラブル

| 症状 | 対処 |
|------|------|
| 502 / 起動しない | Render の Logs を確認。`pip install` 失敗など |
| ログインできない | APP_PASSWORD を Render の Environment で確認 |
| 品目が空 | `data/products.json` がリポジトリに含まれているか確認 |

---

## セキュリティのおすすめ

- パスワードは定期的に変更
- URLは関係者以外に公開しない
- 可能なら店舗ごとにパスワードを共有チャットで送る（メールより安全）
